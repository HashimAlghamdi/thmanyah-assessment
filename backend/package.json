{
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "studio": "npx prisma studio",
    "docker:build": "docker build -t thmanyah-backend .",
    "docker:run": "docker run -p 3001:3001 --env-file .env thmanyah-backend",
    "deploy:build": "npm run build && npx prisma generate",
    "deploy:migrate": "npx prisma db push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "@types/xml2js": "^0.4.14",
    "axios": "^1.10.0",
    "fastify": "^5.4.0",
    "fastify-plugin": "^5.0.1",
    "rss-parser": "^3.13.0",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "prisma": "^6.9.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
