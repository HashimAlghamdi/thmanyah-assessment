version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "No build commands needed for Docker"
run:
  runtime-version: latest
  command: npm start
  network:
    port: 3001
    env: PORT
  env:
    - name: NODE_ENV
      value: production
    - name: PORT
      value: 3001 